fastapi==0.115.6
uvicorn[standard]==0.34.0
qdrant-client==1.12.2
openai==1.59.6
pydantic==2.10.5

# Added by `signoz init` — instrumentation
# traceloop-sdk covers LLM providers and vector DBs, but NOT HTTP frameworks.
# The distro + fastapi instrumentation supply the root server span that LLM and
# vector spans hang off; without it there is no trace, only orphans.
traceloop-sdk==0.38.7
opentelemetry-distro==0.65b0
opentelemetry-instrumentation-fastapi==0.65b0
